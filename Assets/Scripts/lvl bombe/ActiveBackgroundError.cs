using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ActiveBackgroundError : MonoBehaviour
{
    // Start is called before the first frame update
    public void Activer()
    {
        gameObject.SetActive(true);
    }
}
